name: Daily Sync & Updates

on:
  schedule:
    - cron: '0 2 * * 1-5'  # Monday-Friday at 02:00 UTC
  workflow_dispatch:
    inputs:
      test_mode:
        description: 'Run in test mode (no Zoho updates)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      dry_run:
        description: 'Preview changes without uploading (SharePoint sync)'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'
      skip_dashboard:
        description: 'Skip the dashboard data generation step'
        required: false
        default: 'false'
        type: choice
        options:
          - 'true'
          - 'false'

jobs:
  daily-sync:
    name: Daily Sync & Updates
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install boto3 msal requests pandas pytz numpy scipy google-analytics-data matplotlib

      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Restore sync manifest cache
        uses: actions/cache@v5
        with:
          path: .sync_manifest
          key: sharepoint-sync-manifest

      - name: Cache S3 files
        uses: actions/cache@v5
        with:
          path: .cache
          key: s3-cache-${{ runner.os }}-${{ steps.date.outputs.date }}
          restore-keys: |
            s3-cache-${{ runner.os }}-

      # --- Dashboard Data Generation ---
      - name: Generate dashboard data
        if: github.event.inputs.skip_dashboard != 'true'
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          SHAREPOINT_DRIVE_ID: ${{ secrets.SHAREPOINT_DRIVE_ID }}
          GA4_PROPERTY_ID: ${{ secrets.GA4_PROPERTY_ID }}
          GA4_SERVICE_ACCOUNT_KEY: ${{ secrets.GA4_SERVICE_ACCOUNT_KEY }}
          MAILSHAKE_API_KEY: ${{ secrets.MAILSHAKE_API_KEY }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python generate_dashboard_data.py --dry-run --local-dir ./dashboard_output
          else
            python generate_dashboard_data.py
          fi

      # --- S3 → SharePoint Sync ---
      - name: Run S3 to SharePoint sync
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          SHAREPOINT_SITE_ID: ${{ secrets.SHAREPOINT_SITE_ID }}
          SHAREPOINT_DRIVE_ID: ${{ secrets.SHAREPOINT_DRIVE_ID }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            python s3_to_sharepoint.py --dry-run
          else
            python s3_to_sharepoint.py
          fi

      # --- Zoho Industry Sync ---
      - name: Run Zoho Industry Sync
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}
          ZOHO_CLIENT_ID: ${{ secrets.ZOHO_CLIENT_ID }}
          ZOHO_CLIENT_SECRET: ${{ secrets.ZOHO_CLIENT_SECRET }}
          ZOHO_REFRESH_TOKEN: ${{ secrets.ZOHO_REFRESH_TOKEN }}
        run: python zoho_industry.py

      # --- Zoho Tiers Update ---
      - name: Run Tier Update Script
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_KEY }}
          ZOHO_CLIENT_ID: ${{ secrets.ZOHO_CLIENT_ID }}
          ZOHO_CLIENT_SECRET: ${{ secrets.ZOHO_CLIENT_SECRET }}
          ZOHO_REFRESH_TOKEN: ${{ secrets.ZOHO_REFRESH_TOKEN }}
          ZOHO_ORG_ID: ${{ secrets.ZOHO_ORG_ID }}
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}
          AZURE_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
          AZURE_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
          AZURE_SENDER_MAILBOX: ${{ secrets.AZURE_SENDER_MAILBOX }}
          SHAREPOINT_DRIVE_ID: ${{ secrets.SHAREPOINT_DRIVE_ID }}
          TEST_MODE: ${{ github.event.inputs.test_mode || 'false' }}
          TIER_SYSTEM: ${{ vars.TIER_SYSTEM || 'v1' }}
        run: python zoho_tiers.py

      - name: List generated files
        if: always()
        run: |
          echo "=== All CSV files in current directory ==="
          ls -la *.csv || echo "No CSV files found"
          echo ""
          echo "=== Industry revenue report files ==="
          ls -la industry_summary_*.csv *_current_*.csv *_current_new_*.csv 2>/dev/null || echo "No industry revenue files found"

      - name: Upload Tier Report CSV
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: tier-report
          path: tier_updates_*.csv
          retention-days: 30

      - name: Upload Annual Events Report
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: annual-events-report
          path: upcoming_annual_events_*.csv
          retention-days: 30

      - name: Upload Industry Revenue Reports
        uses: actions/upload-artifact@v7
        if: always()
        with:
          name: industry-revenue-reports
          path: |
            industry_summary_*.csv
            *_current_*.csv
            *_current_new_*.csv
          if-no-files-found: warn
          retention-days: 30
