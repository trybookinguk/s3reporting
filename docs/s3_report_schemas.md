# S3 Report Schemas

Reference for every report TryBooking exports to the S3 bucket `produk-rdsextracts-438255373632`. Generated by sampling the live files (June 2026).

> This is the technical, column-level reference. For a plain-English guide to what each report is *for* and the questions it can answer, see **`docs/notion/what_data_we_have.md`**.
>
> Descriptions are filled in where they're well-established; blank `Description` cells are left for completion. Field meanings that aren't obvious from the name should be confirmed against TryBooking's own documentation before relying on them.

## File layout

- **Path format:** `YYYY/MM/YYYYMM-reportname-TBUK.csv`
- **Monthly files** (`YYYYMM-…`) cover the current month and are overwritten daily.
- **Dated files** (`YYYYMMDD-…`) are point-in-time snapshots — `BookingDataAll` (the full history, written periodically) and `AccountMovementDaily` (one per day).
- Most files are a plain CSV with a header row. **Four files have a one-line metadata preamble** before the real header (see the ⚠️ note on those).

## Which reports the pipeline actually uses

| Report | Used by | Notes |
|---|---|---|
| Accounts | `prepare_data.py`, tiers, most reports | Core account master data |
| BookingData | `prepare_data.py` (daily upsert) | Current-month transactions |
| BookingDataAll | `prepare_data.py` (history seed) | Full transaction history (~2.2 GB) |
| Users | `prepare_data.py`, domain/commission reports | |
| RiskReport | event completion reminders, funds reminders | Balances + exposure |
| AccountBalance | (legacy / ad-hoc) | Superseded by RiskReport for most uses |
| AccountMovementDaily | funds/event reminders | Daily ledger movements incl. `Pending` |
| The rest | not currently consumed | Available if needed |

---

## Core reports

### Accounts — `YYYYMM-Accounts-TBUK.csv` (37 cols)

Account master data: identity, status, fees, tier/rating, location. **One row per account.**

| Column | Description |
|---|---|
| `Id` | The account's ID on TryBooking. (Note: `Id` here, not `AccountId`.) |
| `AccountName` | The account's name. |
| `AccountStatus` | Account status (e.g. Activated, Closed). |
| `DateTimeCreated` | When the account was created. |
| `IsVerified` | Whether the account is verified. |
| `BankAccountIsDeleted` | Whether the bank account has been removed. |
| `BankAccountName` | Bank account name (hashed/encrypted in the export). |
| `BSB` | Bank-state-branch code (hashed/encrypted). |
| `AccNumber` | Bank account number (hashed/encrypted). |
| `BankAccountStatusId` | Bank account status code. |
| `LastLogIn` | Last login to the account. |
| `FirstEventCreation` | When the account first created an event. |
| `LastEventCreation` | When the account most recently created an event. |
| `Industry` | Industry assigned to the account. |
| `SubIndustry` | Sub-industry assigned to the account. |
| `GatewayGroup` | Payment gateway assigned to the account. |
| `DGRStatus` | Not used in UK. |
| `IsGstEnabled` | Whether GST is enabled (AU concept). |
| `ProcessingFeeOnline` |  |
| `ProcessingFeeBoxOffice` |  |
| `TicketFeeOnline` |  |
| `TicketFeeBoxOffice` |  |
| `DonationFeeOnline` |  |
| `DonationFeeBoxOffice` |  |
| `TierSystem` | Which tier system the account is on (v1/v2). |
| `PreviousTier` | The account's prior tier. |
| `YearsLoyalty` | Years the account has been active. |
| `TicketQuantity` |  |
| `AccountRating` | Current activity rating. |
| `PreviousRating` | Prior activity rating. |
| `RatingChanged` | Whether the rating changed. |
| `State` |  |
| `Postcode` | Account postcode (from account settings). |
| `Country` |  |
| `SchoolRecNumber` |  |
| `PromoCode` |  |
| `LastAccountTransactionDate` | Date of the account's last transaction. |

---

### BookingData — `YYYYMM-BookingData-TBUK.csv` (39 cols)

Current-month ticket transactions, refreshed daily. **One row per transaction.** The most-used report.

| Column | Description |
|---|---|
| `BookingId` | Internal platform use only. |
| `BookingTransactionId` | Internal platform use only. |
| `AccountId` | The ID of the account on TryBooking. |
| `AccountName` | The name of the account. |
| `DateTimeCreated` | When the account was created. |
| `EventId` | The ID of the event. |
| `EventName` | The name of the event. |
| `DonationCampaignId` | Not used in UK. |
| `DonationCampaignName` | Not used in UK. |
| `TransactionDate` | When the transaction took place. |
| `BookingUrlId` | External booking ID. |
| `PaymentReceived` | Price of all tickets in the transaction (excluding fees). |
| `TicketQuantity` | Number of tickets in the transaction. |
| `BookingFee` | Ticket fee paid by the event organiser. |
| `CardFee` | Processing fee paid by the event organiser. |
| `ProcessingFee` | Processing fee paid by the ticket purchaser. |
| `Surcharge` | No longer used in UK. |
| `ProcessingFeeSurcharge` | No longer used in UK. |
| `TicketFee` | Ticket fee paid by the ticket purchaser. |
| `TransactionType` | Appears to always be "Payment". |
| `PaymentType` | Payment type used (paid tickets only). |
| `EventPostcode` | Postcode on the event's venue details. |
| `AccountPostcode` | Postcode from the account's settings. |
| `EventDate` | Session date/time of the event the tickets were for. |
| `Industry` | Industry assigned to the account. |
| `SubIndustry` | Sub-industry assigned to the account. |
| `GatewayGroup` | Gateway assigned to the account. |
| `DGRStatus` | Not used in UK. |
| `CustomerId` | Internal customer ID. |
| `IPCountry` | Country of the transaction, by IP. |
| `Status` | Successful, Failed or Unknown. |
| `Wallet` | Mobile wallet (Apple Pay / Google Pay) — Stripe only. |
| `GatewayName` | Payment gateway used. |
| `GatewayId` | Payment reference within Stripe/PayPal. |
| `GatewayReference` | Not used in UK. |
| `GiftCertificateTypeName` | Gift certificate name (if used). |
| `GiftCertificateId` | Gift certificate ID (if used). |
| `BookingCountryCode` | Always GBR. |
| `IPAddress` | Buyer's IP address. |

---

### BookingDataAll — `YYYYMMDD-BookingDataAll-TBUK.csv` (39 cols)

**Identical schema to BookingData** (see above), but the **full transaction history** to the file's date (~2.2 GB). Written periodically as a dated snapshot.

---

### Users — `YYYYMM-users-TBUK.csv` (15 cols)

User accounts and roles. **One row per person**; an account can have several users.

| Column | Description |
|---|---|
| `UserId` | The user's ID. |
| `RoleId` | Role ID. |
| `RoleName` | Role name; the account owner has `AccountOwner`. |
| `Username` | The user's login email. |
| `FirstName` | First name. |
| `LastName` | Last name. |
| `IsDeleted` | Whether the user has been deleted. |
| `AccountId` | The account this user belongs to. |
| `Unsubscribed` | Whether the user has unsubscribed from email. |
| `ProviderName` | Identity provider (if applicable). |
| `SmsMultiFactorEnabled` | Whether SMS MFA is on. |
| `SecondFactorEnabled` | Whether 2FA is on. |
| `MfaReminderDate` |  |
| `LastLoginDatetime` | Last login. |
| `LastPasswordChangeDate` | Last password change. |

---

## Balance & risk

### RiskReport — `YYYYMM-RiskReport-TBUK.csv` (8 cols)

Per-account balance and exposure — used to decide whether funds can be paid out. **One row per account.**

| Column | Description |
|---|---|
| `AccountId` | The account's ID. |
| `AccountName` | The account's name. |
| `FullBalance` | Total balance, including pending. |
| `Balance` | Available balance (excludes pending). |
| `SalesForUpcomingEvents` | Revenue tied to events that haven't happened yet. |
| `Exposure` | TryBooking's own exposure flag/figure. |
| `FutureDays` | Days until the furthest upcoming event. |
| `LastEventDate` | Date of the account's last event. |

---

### AccountBalance — `YYYYMM-accountbalance-TBUK.csv` (8 cols)

Simpler per-account balance snapshot. Largely superseded by RiskReport.

| Column | Description |
|---|---|
| `AccountID` | The account's ID. |
| `AccountName` | The account's name. |
| `AccountCreationDate` | When the account was created. |
| `AccountBalance` | The account's balance. |
| `Industry` | Industry assigned to the account. |
| `SubIndustry` | Sub-industry assigned to the account. |
| `GatewayGroup` | Gateway assigned to the account. |
| `DGRStatus` | Not used in UK. |

---

### Account-Transactions — `YYYYMM-Account-Transactions-TBUK.csv` (10 cols)

Ledger of debits/credits against accounts (billing line items). **One row per ledger entry.**

| Column | Description |
|---|---|
| `Id` | Ledger entry ID. |
| `AccountID` | The account the entry is against. |
| `Description` | Free-text description of the line item. |
| `DebitAmount` | Amount debited. |
| `CreditAmount` | Amount credited. |
| `TransactionDate` | When the entry was made. |
| `IsAdditionalBilling` | Whether this is additional billing. |
| `TransactionTypeId` | Transaction type code. |
| `TransactionType` | Transaction type (e.g. Booking). |
| `BookingId` | The related booking, if any. |

---

## Movement / reconciliation ⚠️ (preamble files)

> **These four files start with a one-line metadata preamble**, *then* the real header on line 2. The preamble describes the gateway-batch range the file covers: `First Gateway ID, …, Start Booking Transaction ID, …, Last Gateway ID, …, End Booking Transaction ID, …`. When parsing, **skip line 1** (or detect that the real header begins with `AccountId` / `PaymentGatewayId` / `BookingTransactionId`).

### AccountMovement — `YYYYMM-AccountMovement-TBUK.csv` (43 cols)

Full account ledger movement (all-time totals per account) — ticket value, fees, refunds, gift certificates, balances. **One row per account.**

| Column | Description |
|---|---|
| `AccountId` | The account's ID. |
| `DateTimeCreated` | When the account was created. |
| `AccountName` | The account's name. |
| `TicketValue` |  |
| `FundraisingDonation` |  |
| `Donation` |  |
| `BoxOfficeFee` |  |
| `TotalBookingFee` |  |
| `TotalSurcharge` |  |
| `TotalProcessingFeeSurcharge` |  |
| `TotalCardFee` |  |
| `TotalTicketFee` |  |
| `TotalProcessingFee` |  |
| `TotalDonationFee` |  |
| `GCUsed` | Gift certificate amount used. |
| `TicketRefundedAmount` |  |
| `FundraisingDonationRefunded` |  |
| `DonationRefundedAmount` |  |
| `BoxOfficeFeeRefundedAmount` |  |
| `TotalRefundedCardFee` |  |
| `TotalRefundedTicketFee` |  |
| `TotalRefundedProcessingFee` |  |
| `TotalRefundedSurcharge` |  |
| `TotalRefundedPFSurcharge` |  |
| `TotalRefundedDonationFee` |  |
| `ReturnedGCValue` |  |
| `RefundedGCValue` |  |
| `GCValue` |  |
| `GCDiscount` |  |
| `AdditionalCreditBilling` |  |
| `AdditionalDebitBilling` |  |
| `DebitListing` |  |
| `FundTransafer` | Funds transferred out. (Misspelled in source.) |
| `BankVerfication` | Bank verification. (Misspelled in source.) |
| `Recredit` |  |
| `Pending` | Amount currently in a transfer state. Used by the funds/event reminder logic. |
| `OpeningBalance` |  |
| `ClosingBalance` |  |
| `Industry` | Industry assigned to the account. |
| `SubIndustry` | Sub-industry assigned to the account. |
| `GatewayGroup` | Gateway assigned to the account. |
| `DGRStatus` | Not used in UK. |
| `StripeTransfers` |  |

---

### AccountMovementDaily — `YYYYMMDD-AccountMovementDaily-TBUK.csv` (43 cols)

**Same 43-column schema as AccountMovement** (see above), but written **once per day** as a dated snapshot.

---

### GatewayMovement — `YYYYMM-GatewayMovement-TBUK.csv` (32 cols)

Same kind of movement totals but grouped **by payment gateway** instead of by account. **One row per gateway.**

| Column | Description |
|---|---|
| `PaymentGatewayId` | Gateway ID. |
| `PaymentGatewayName` | Gateway name (e.g. Stripe, PayPal). |
| `TicketValue` |  |
| `FundraisingDonation` |  |
| `Donation` |  |
| `BoxOfficeFee` |  |
| `TotalBookingFee` |  |
| `TotalSurcharge` |  |
| `TotalProcessingFeeSurcharge` |  |
| `TotalCardFee` |  |
| `TotalTicketFee` |  |
| `TotalProcessingFee` |  |
| `TotalDonationFee` |  |
| `GCUsed` |  |
| `TicketRefundedAmount` |  |
| `FundraisingDonationRefunded` |  |
| `DonationRefundedAmount` |  |
| `BoxOfficeFeeRefundedAmount` |  |
| `TotalRefundedCardFee` |  |
| `TotalRefundedTicketFee` |  |
| `TotalRefundedProcessingFee` |  |
| `TotalRefundedSurcharge` |  |
| `TotalRefundedPFSurcharge` |  |
| `TotalRefundedDonationFee` |  |
| `ReturnedGCValue` |  |
| `RefundedGCValue` |  |
| `GCValue` |  |
| `GCDiscount` |  |
| `DebitListing` |  |
| `OpeningBalance` |  |
| `ClosingBalance` |  |
| `StripeTransfers` |  |

---

### TransactionReconciliation — `YYYYMM-TransactionReconciliation-TBUK.csv` (13 cols)

Per-transaction reconciliation against the payment gateway — card, amount, payout. **One row per transaction.**

| Column | Description |
|---|---|
| `BookingTransactionId` | The transaction ID. |
| `TransactionDate` | When the transaction took place. |
| `GatewayId` | Payment reference within the gateway. |
| `BookingUrlId` | External booking ID. |
| `CardType` | Card type (VISA, MasterCard, etc.). |
| `CardNo` | Last 4 digits of the card only. |
| `Amount` | Transaction amount. |
| `Transaction` | Transaction type (e.g. Payment). |
| `GatewayMessage` | Message returned by the gateway. |
| `PayoutId` | Payout the transaction was settled in. |
| `ArrivalDate` | When funds arrived. |
| `Provider` | Payment provider (Stripe, Braintree PayPal, etc.). |
| `AccountName` | The account's name. |

---

## Donations & other

### Transfers — `YYYYMM-transfers-TBUK.csv` (4 cols)

Bank transfer records. **One row per transfer.**

| Column | Description |
|---|---|
| `Amount` | Transfer amount. |
| `DateCreated` | When the transfer was created. |
| `Filename` | Source file the transfer came from. |
| `AccountId` | The account the transfer is for. |

---

### DonationCampaigns — `YYYYMM-donationcampaigns-TBUK.csv` (8 cols)

Donation campaign pages (not used in the UK historically). **One row per campaign.**

| Column | Description |
|---|---|
| `Id` | Campaign ID. |
| `PageUrlHandle` | URL handle for the campaign page. |
| `AccountId` | The owning account. |
| `IsActive` | Whether the campaign is active. |
| `PageName` | Campaign page name. |
| `Description` | Campaign description. |
| `ContactEmailAddress` | Contact email. |
| `DateTimeCreated` | When the campaign was created. |

---

### Donations — `YYYYMM-donations-TBUK.csv` (17 cols)

Donation transactions. **One row per donation.**

| Column | Description |
|---|---|
| `BookingId` | Internal platform use only. |
| `BookingTransactionId` | Internal platform use only. |
| `AccountId` | The account's ID. |
| `AccountName` | The account's name. |
| `DateTimeCreated` | When the account was created. |
| `EventId` | The event ID. |
| `EventName` | The event name. |
| `TransactionDate` | When the donation was made. |
| `BookingFirstName` | Donor first name. |
| `BookingLastName` | Donor last name. |
| `BookingUrlId` | External booking ID. |
| `Donation` | Donation amount. |
| `PaymentType` | Payment type used. |
| `Industry` | Industry assigned to the account. |
| `SubIndustry` | Sub-industry assigned to the account. |
| `GatewayGroup` | Gateway assigned to the account. |
| `DGRStatus` | Not used in UK. |

---

### ScanAppUsers — `YYYYMM-scanapp-users-TBUK.csv` (6 cols)

Lists the users who have used the ticket-scanning app, by event. **One row per account/event.**

| Column | Description |
|---|---|
| `AccountId` | The account's ID. |
| `AccountName` | The account's name. |
| `EventId` | The event ID. |
| `EventName` | The event name. |
| `TotalDevices` | Number of devices used to scan. |
| `TotalScanned` | Number of tickets scanned. |
